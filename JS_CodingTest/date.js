const nowDate = new Date();
const date = Date()

console.log(nowDate)
console.log(date)

const bool = new Boolean(true)
const num = new Number('gkdl')
console.log(bool)
console.log(num)
console.log(Boolean(''))

const literal = 10
const number = new Number(10)
console.log(literal, literal instanceof Number)
console.log(number, number instanceof Number)